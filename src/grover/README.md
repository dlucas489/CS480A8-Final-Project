# src/grover/

Grover's Algorithm circuit construction.

Planned modules:
- `circuit.py` — full Grover circuit (oracle + diffuser, configurable qubit count and iterations)
- `oracle.py`  — phase-flip oracle marking the target state
- `diffuser.py` — Grover diffusion (inversion about average) operator
