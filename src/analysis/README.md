# src/analysis/

Post-simulation analysis and visualization.

Planned modules:
- `success_probability.py` — compute empirical and theoretical success probabilities
- `threshold.py`           — detect the circuit depth at which Grover loses classical advantage
- `plots.py`               — generate publication-ready figures (depth vs. P_success, platform ranking)
